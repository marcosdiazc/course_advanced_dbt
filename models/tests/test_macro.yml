models:
  - name: test_macro
    description: This model contains a macro for calculations

    columns:
      - name: square_number
        description: this a test of a macro
        tests:
          - not_null
          - unique
